as yet unclassified
start
	"bibtex is a subset of command, its a command with a list the list itself contains key value pairs as parameters and one string."
 ^ (command, $, asParser optional) nonEmpty star